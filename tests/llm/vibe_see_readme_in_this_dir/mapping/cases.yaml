subfield: mapping
cases:
  - id: mapping_smoke_parse
    prompt: "Load representative inputs for mapping and ensure parsing or object construction succeeds."
    expected: "no_error"
  - id: mapping_smoke_execute
    prompt: "Execute one representative code path for mapping and ensure it returns a non-empty result."
    expected: "no_error"
