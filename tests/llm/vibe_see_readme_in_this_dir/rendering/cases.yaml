subfield: rendering
cases:
  - id: rendering_smoke_parse
    prompt: "Load representative inputs for rendering and ensure parsing or object construction succeeds."
    expected: "no_error"
  - id: rendering_smoke_execute
    prompt: "Execute one representative code path for rendering and ensure it returns a non-empty result."
    expected: "no_error"
