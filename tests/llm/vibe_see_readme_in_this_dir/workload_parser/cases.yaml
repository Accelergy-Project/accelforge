subfield: workload_parser
cases:
  - id: workload_parser_smoke_parse
    prompt: "Load representative inputs for workload_parser and ensure parsing or object construction succeeds."
    expected: "no_error"
  - id: workload_parser_smoke_execute
    prompt: "Execute one representative code path for workload_parser and ensure it returns a non-empty result."
    expected: "no_error"
