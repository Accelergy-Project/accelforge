subfield: api_gaps
cases:
  - id: api_gaps_smoke_parse
    prompt: "Load representative inputs for api_gaps and ensure parsing or object construction succeeds."
    expected: "no_error"
  - id: api_gaps_smoke_execute
    prompt: "Execute one representative code path for api_gaps and ensure it returns a non-empty result."
    expected: "no_error"
