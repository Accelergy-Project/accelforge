subfield: workload_model
cases:
  - id: workload_model_smoke_parse
    prompt: "Load representative inputs for workload_model and ensure parsing or object construction succeeds."
    expected: "no_error"
  - id: workload_model_smoke_execute
    prompt: "Execute one representative code path for workload_model and ensure it returns a non-empty result."
    expected: "no_error"
