subfield: renames
cases:
  - id: renames_smoke_parse
    prompt: "Load representative inputs for renames and ensure parsing or object construction succeeds."
    expected: "no_error"
  - id: renames_smoke_execute
    prompt: "Execute one representative code path for renames and ensure it returns a non-empty result."
    expected: "no_error"
