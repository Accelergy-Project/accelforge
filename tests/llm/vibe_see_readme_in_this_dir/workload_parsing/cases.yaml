subfield: workload_parsing
cases:
  - id: workload_parsing_smoke_parse
    prompt: "Load representative inputs for workload_parsing and ensure parsing or object construction succeeds."
    expected: "no_error"
  - id: workload_parsing_smoke_execute
    prompt: "Execute one representative code path for workload_parsing and ensure it returns a non-empty result."
    expected: "no_error"
