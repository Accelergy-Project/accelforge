subfield: arch_flattening
cases:
  - id: arch_flattening_smoke_parse
    prompt: "Load representative inputs for arch_flattening and ensure parsing or object construction succeeds."
    expected: "no_error"
  - id: arch_flattening_smoke_execute
    prompt: "Execute one representative code path for arch_flattening and ensure it returns a non-empty result."
    expected: "no_error"
