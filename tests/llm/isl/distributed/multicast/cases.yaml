subfield: multicast
cases:
  - id: multicast_smoke_parse
    prompt: "Load representative inputs for multicast and ensure parsing or object construction succeeds."
    expected: "no_error"
  - id: multicast_smoke_execute
    prompt: "Execute one representative code path for multicast and ensure it returns a non-empty result."
    expected: "no_error"
