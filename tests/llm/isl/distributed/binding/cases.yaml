subfield: binding
cases:
  - id: binding_smoke_parse
    prompt: "Load representative inputs for binding and ensure parsing or object construction succeeds."
    expected: "no_error"
  - id: binding_smoke_execute
    prompt: "Execute one representative code path for binding and ensure it returns a non-empty result."
    expected: "no_error"
