subfield: model
cases:
  - id: model_smoke_parse
    prompt: "Load representative inputs for model and ensure parsing or object construction succeeds."
    expected: "no_error"
  - id: model_smoke_execute
    prompt: "Execute one representative code path for model and ensure it returns a non-empty result."
    expected: "no_error"
