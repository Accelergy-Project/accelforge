subfield: plotting
cases:
  - id: plotting_smoke_parse
    prompt: "Load representative inputs for plotting and ensure parsing or object construction succeeds."
    expected: "no_error"
  - id: plotting_smoke_execute
    prompt: "Execute one representative code path for plotting and ensure it returns a non-empty result."
    expected: "no_error"
