'0.0':
  What is your name?:
    answer: Claude
    assumptions: []
    weight: 1
'0.1':
  What is your email address?:
    answer: claude@mit.edu
    assumptions: []
    weight: 1
'0.2':
  What is your kerberos?:
    answer: claude
    assumptions: []
    weight: 1
'1.1':
  Your matrix multiplication implementation.:
    answer:
      callable: my_matrix_multiplication
      source: "def my_matrix_multiplication(\n    A_list: list[Number],\n    B_list: list[Number],\n    M: int,\n    K: int,\n    N: int\n) -> list[Number]:\n    \"\"\"\n    Returns the result of multiplying matrices A and B represented in lists as\n    A_list and B_list. Matrix A has M rows and K columns, and matrix B has K\n    rows and N columns. The elements of the matrices are laid out \"row-major\"\n    in the lists:\n        A[m, k] == A_list[K*m+k]\n    The result matrix, which has M rows and N columns, should also be returned\n    as a list following row-major layout.\n    \"\"\"\n    result = [0] * (M * N)\n    for m in range(M):\n        for n in range(N):\n            s = 0\n            for k in range(K):\n                s += A_list[K * m + k] * B_list[N * k + n]\n            result[N * m + n] = s\n    return result"
    assumptions: []
    weight: 1
'2.1':
  AccelForge works!:
    answer: true
    assumptions: []
    weight: 1
