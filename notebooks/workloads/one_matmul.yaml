problem:
  - shape:
      name: Fc1
      dimensions: [ M1, N1, K1 ]
      data_spaces:
      - {name: Fmap1,   dimensions: [ Fmap1_K, Fmap1_M ],     projection: '[ K1, M1 ]'}
      - {name: Filter1, dimensions: [ Filter1_K, Filter1_N ], projection: '[ K1, N1 ]'}
      - {name: Fmap2,   dimensions: [ Fmap2_K, Fmap2_M ],     projection: '[ N1, M1 ]', read_write: True}
    instance: 0 <= M1 < 512 and 0 <= N1 < 512 and 0 <= K1 < 512

  # - shape:
  #     name: Fc2
  #     dimensions: [ M2, N2, K2 ]
  #     data_spaces:
  #     - {name: Fmap2,   dimensions: [ Fmap2_K, Fmap2_M ],     projection: '[ K2, M2 ]'}
  #     - {name: Filter2, dimensions: [ Filter2_K, Filter2_N ], projection: '[ K2, N2 ]'}
  #     - {name: Fmap3,   dimensions: [ Fmap3_K, Fmap3_M ],     projection: '[ N2, M2 ]', read_write: True}
  #   instance: 0 <= M2 < 128 and 0 <= N2 < 128 and 0 <= K2 < 128

  # - shape:
  #     name: Fc3
  #     dimensions: [ M3, N3, K3 ]
  #     data_spaces:
  #     - {name: Fmap3,   dimensions: [ Fmap3_K, Fmap3_M ],     projection: '[ K3, M3 ]'}
  #     - {name: Filter3, dimensions: [ Filter3_K, Filter3_N ], projection: '[ K3, N3 ]'}
  #     - {name: Fmap4,   dimensions: [ Fmap4_K, Fmap4_M ],     projection: '[ N3, M3 ]', read_write: True}
  #   instance: 0 <= M3 < 128 and 0 <= N3 < 128 and 0 <= K3 < 128

  # - shape:
  #     name: Fc4
  #     dimensions: [ M4, N4, K4 ]
  #     data_spaces:
  #     - {name: Fmap4,   dimensions: [ Fmap4_K, Fmap4_M ],     projection: '[ K4, M4 ]'}
  #     - {name: Filter4, dimensions: [ Filter4_K, Filter4_N ], projection: '[ K4, N4 ]'}
  #     - {name: Fmap5,   dimensions: [ Fmap5_K, Fmap5_M ],     projection: '[ N4, M4 ]', read_write: True}
  #   instance: 0 <= M4 < 128 and 0 <= N4 < 128 and 0 <= K4 < 128

  # - shape:
  #     name: Fc5
  #     dimensions: [ M5, N5, K5 ]
  #     data_spaces:
  #     - {name: Fmap5,   dimensions: [ Fmap5_K, Fmap5_M ],     projection: '[ K5, M5 ]'}
  #     - {name: Filter5, dimensions: [ Filter5_K, Filter5_N ], projection: '[ K5, N5 ]'}
  #     - {name: Fmap6,   dimensions: [ Fmap6_K, Fmap6_M ],     projection: '[ N5, M5 ]', read_write: True}
  #   instance: 0 <= M5 < 128 and 0 <= N5 < 128 and 0 <= K5 < 128

  # - shape:
  #     name: Fc6
  #     dimensions: [ M6, N6, K6 ]
  #     data_spaces:
  #     - {name: Fmap6,   dimensions: [ Fmap6_K, Fmap6_M ],     projection: '[ K6, M6 ]'}
  #     - {name: Filter6, dimensions: [ Filter6_K, Filter6_N ], projection: '[ K6, N6 ]'}
  #     - {name: Fmap7,   dimensions: [ Fmap7_K, Fmap7_M ],     projection: '[ N6, M6 ]', read_write: True}
  #   instance: 0 <= M6 < 128 and 0 <= N6 < 128 and 0 <= K6 < 128
